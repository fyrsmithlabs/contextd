global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'contextd-test'
    static_configs:
      # WSL2 eth0 IP - update if WSL restarts
      - targets: ['172.30.163.84:9090']
